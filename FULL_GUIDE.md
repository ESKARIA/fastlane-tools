# 📚 Полное руководство по использованию Fastlane Tools

## 📋 Содержание

1. [Введение](#введение)
2. [Установка и настройка](#установка-и-настройка)
3. [Структура проекта](#структура-проекта)
4. [Основные концепции](#основные-концепции)
5. [Детальное описание всех lanes](#детальное-описание-всех-lanes)
6. [Workflow примеры](#workflow-примеры)
7. [Переменные окружения](#переменные-окружения)
8. [Troubleshooting](#troubleshooting)
9. [Best Practices](#best-practices)

---

## 🎯 Введение

Этот репозиторий содержит набор модульных инструментов для автоматизации процессов разработки и публикации iOS приложений с помощью Fastlane. Все Fastfile разделены на логические модули для удобства поддержки и повторного использования.

### Основные возможности:

- ✅ **Управление сертификатами** - автоматическое управление сертификатами и профилями через `match`
- ✅ **Сборка приложений** - автоматическая сборка IPA файлов для App Store
- ✅ **Тестирование** - запуск unit и UI тестов
- ✅ **Загрузка в TestFlight** - автоматическая загрузка сборок в TestFlight (internal и external testers)
- ✅ **Публикация в App Store** - отправка приложений на рецензирование
- ✅ **Управление версиями** - автоматическое управление версиями и build numbers
- ✅ **Загрузка символов** - автоматическая загрузка dSYM в AppMetrica и Firebase Crashlytics

---

## 🚀 Установка и настройка

### Предварительные требования

1. **Xcode** - установлен и настроен
2. **Ruby** - версия 2.7 или выше
3. **Bundler** - для управления зависимостями
4. **Fastlane** - установлен через Bundler или system-wide

### Установка

```bash
# 1. Клонируйте репозиторий или используйте import_from_git
git clone git@github.com:ESKARIA/fastlane-tools.git

# 2. Установите зависимости
bundle install

# 3. Настройте переменные окружения (см. раздел "Переменные окружения")
```

### Первоначальная настройка

1. **Создайте App Store Connect API ключ:**
   - Перейдите в [App Store Connect](https://appstoreconnect.apple.com)
   - Откройте Users and Access → Keys
   - Создайте новый API ключ с правами App Manager или Admin
   - Скачайте `.p8` файл и сохраните Key ID и Issuer ID

2. **Настройте переменные окружения:**
   ```bash
   export APP_IDENTIFIER="com.company.appname"
   export MAIN_TARGET="AppName"
   export APPSTORE_KEY_ID="ABC123XYZ"
   export APPSTORE_ISSUER_ID="12345678-1234-1234-1234-123456789012"
   export APPSTORE_KEY_CONTENT="$(cat path/to/key.p8 | base64)"
   ```

3. **Настройте match:**
   ```bash
   # Инициализация match
   fastlane match init
   
   # Первая генерация сертификатов
   fastlane match_generate_appstore
   ```

---

## 📁 Структура проекта

```
fastlane-tools/
├── fastlane/
│   ├── Fastfile                 # Главный файл конфигурации
│   ├── Fastfile_match          # Управление сертификатами и профилями
│   ├── Fastfile_build          # Сборка приложений
│   ├── Fastfile_tests          # Запуск тестов
│   ├── Fastfile_dsyms         # Загрузка символов
│   ├── Fastfile_upload         # Загрузка в TestFlight
│   ├── Fastfile_appstore       # Работа с App Store
│   └── Fastfile_create_app     # Создание нового приложения
├── Gemfile                      # Ruby зависимости
└── README.md                    # Основная документация
```

### Описание модулей:

#### **Fastfile** (главный)
- Определяет константы и переменные окружения
- Импортирует все модульные Fastfile
- Содержит общие lanes и хуки

#### **Fastfile_match**
- Управление сертификатами и provisioning profiles
- Генерация и установка сертификатов через `match`

#### **Fastfile_build**
- Сборка IPA файлов для App Store
- Управление версиями и build numbers

#### **Fastfile_upload**
- Загрузка сборок в TestFlight
- Поддержка internal и external testers

#### **Fastfile_appstore**
- Публикация приложений в App Store
- Управление метаданными и версиями

#### **Fastfile_dsyms**
- Загрузка dSYM файлов в AppMetrica и Firebase

#### **Fastfile_tests**
- Запуск unit и UI тестов

#### **Fastfile_create_app**
- Создание нового приложения в App Store Connect

---

## 💡 Основные концепции

### Lanes (Лейны)

Lane - это отдельная задача или процесс в fastlane. Каждый lane можно запустить независимо:

```bash
fastlane <lane_name>
```

### Options (Параметры)

Lanes могут принимать параметры через опции:

```bash
fastlane upload_external_testflight groups:"Group1,Group2"
```

### Environment Variables (Переменные окружения)

Большинство настроек передаются через переменные окружения для гибкости и безопасности.

### Match

Match - инструмент для управления сертификатами и профилями через git репозиторий. Все сертификаты шифруются и хранятся в централизованном репозитории.

---

## 📖 Детальное описание всех lanes

### 🔐 Управление сертификатами (Fastfile_match)

#### `match_generate_dev`
Генерирует development сертификаты и provisioning profiles.

```bash
fastlane match_generate_dev
```

**Что делает:**
- Создает/обновляет development сертификаты
- Генерирует provisioning profiles для development
- Сохраняет в git репозиторий match (ветка `development`)

**Когда использовать:**
- При добавлении нового устройства для разработки
- При обновлении списка устройств
- При первом запуске проекта

---

#### `match_generate_appstore`
Генерирует App Store сертификаты и provisioning profiles.

```bash
fastlane match_generate_appstore
```

**Что делает:**
- Создает/обновляет App Store сертификаты
- Генерирует provisioning profiles для App Store distribution
- Сохраняет в git репозиторий match (ветка `distribution`)

**Когда использовать:**
- Перед первой сборкой для App Store
- При необходимости обновить сертификаты

---

#### `match_install_dev`
Устанавливает development сертификаты и профили локально.

```bash
fastlane match_install_dev
```

**Что делает:**
- Клонирует сертификаты из git репозитория
- Устанавливает их в локальный keychain
- Устанавливает provisioning profiles

**Когда использовать:**
- Перед локальной разработкой
- При настройке нового компьютера разработчика
- При сбоях с сертификатами

---

#### `match_install_appstore`
Устанавливает App Store сертификаты и профили локально.

```bash
fastlane match_install_appstore
```

**Что делает:**
- Клонирует App Store сертификаты из git репозитория
- Устанавливает их в локальный keychain
- Устанавливает provisioning profiles для distribution

**Когда использовать:**
- Перед сборкой для App Store
- При настройке CI/CD

---

#### `reset_all_profiles`
Полностью перегенерирует все сертификаты и профили.

```bash
fastlane reset_all_profiles
```

**⚠️ Внимание:** Это действие удалит и пересоздаст все сертификаты. Используйте с осторожностью!

---

#### `nuke_all`
Полностью удаляет все сертификаты и профили (разрушительная операция).

```bash
fastlane nuke_all
```

**⚠️ Внимание:** Это необратимое действие! Используйте только при необходимости полностью очистить все сертификаты.

---

### 🏗️ Сборка приложений (Fastfile_build)

#### `version`
Устанавливает маркетинговую версию приложения.

```bash
fastlane version
```

**Требует переменную:**
- `APP_VERSION` - новая версия (например, "1.2.3")

---

#### `build`
Собирает IPA файл для App Store.

```bash
fastlane build
```

**Что делает:**
1. Проверяет обязательные переменные окружения
2. Устанавливает версию приложения
3. Инкрементирует build number
4. Устанавливает App Store сертификаты через match
5. Собирает приложение с подписью для App Store
6. Сохраняет IPA и dSYM файлы

**Требует переменные:**
- `APP_VERSION` - версия приложения
- `CI_PIPELINE_IID` или `BUILD_NUMBER` - номер сборки
- `APP_IDENTIFIER` - bundle identifier
- `MAIN_TARGET` - название target
- `APPSTORE_KEY_ID`, `APPSTORE_ISSUER_ID`, `APPSTORE_KEY_CONTENT` - API ключи

**Результат:**
- IPA файл: `~/.cache/apps/{TARGET}/ipa/{TARGET}_v{VERSION}_b{BUILD}.ipa`
- dSYM файл: `~/.cache/apps/{TARGET}/dsyms/{TARGET}_v{VERSION}_b{BUILD}.app.dSYM.zip`

---

### 🧪 Тестирование (Fastfile_tests)

#### `tests`
Запускает unit тесты.

```bash
fastlane tests scheme:"AppName" device:"iPhone 13"
```

**Параметры:**
- `scheme` - схема для тестирования
- `device` - симулятор для тестирования

**Что делает:**
- Запускает тесты на указанном симуляторе
- Генерирует отчет о покрытии кода
- Сохраняет результаты тестирования

---

### 📦 Загрузка символов (Fastfile_dsyms)

#### `upload_dsyms`
Загружает dSYM файлы в AppMetrica и/или Firebase.

```bash
# Только AppMetrica
fastlane upload_dsyms

# AppMetrica + Firebase
fastlane upload_dsyms firebase:true
```

**Что делает:**
- Ищет dSYM.zip файлы в проекте
- Распаковывает архивы
- Загружает в AppMetrica (если настроено)
- Загружает в Firebase Crashlytics (если указано `firebase:true`)

**Требует переменные:**
- `APPMETRICA_KEY` - ключ API AppMetrica (для AppMetrica)
- `APPMETRICA_HELPER_PATH` - путь к helper скрипту (для AppMetrica)
- `UPLOAD_DSYMS` - флаг включения загрузки (по умолчанию включено)

---

### 📤 Загрузка в TestFlight (Fastfile_upload)

#### `upload_testflight`
Загружает сборку в TestFlight для **internal testers**.

```bash
fastlane upload_testflight
```

**Что делает:**
1. Находит IPA файлы в директории `fastlane/artifacts/`
2. Генерирует changelog из git коммитов
3. Загружает сборку в TestFlight
4. Распределяет для internal testers

**Требует:**
- IPA файлы в `fastlane/artifacts/`
- API ключи App Store Connect

**Результат:**
- Сборка доступна для internal testers немедленно

---

#### `upload_external_testflight` ⭐ НОВЫЙ
Загружает сборку в TestFlight для **external testers**.

```bash
# С группой по умолчанию
fastlane upload_external_testflight

# С указанием групп
fastlane upload_external_testflight groups:"External Public Beta,QA Testers"

# Через переменную окружения
EXTERNAL_TESTFLIGHT_GROUPS="External Public Beta" fastlane upload_external_testflight
```

**Что делает:**
1. Находит IPA файлы в директории `fastlane/artifacts/`
2. Генерирует changelog из git коммитов
3. Загружает сборку в TestFlight
4. Распределяет для external testers в указанные группы

**Параметры:**
- `groups` - группы external testers (через запятую) или переменная `EXTERNAL_TESTFLIGHT_GROUPS`

**Важно:**
- Группы должны существовать в App Store Connect
- Первая сборка версии требует рецензирования Apple (24-48 часов)
- Changelog обязателен для external testers

**Результат:**
- Сборка отправлена на рецензирование Apple (для первой версии)
- После одобрения доступна для external testers

---

#### `tagging`
Создает git тег с версией и build number.

```bash
fastlane tagging tags:true
```

**Параметры:**
- `tags` - если `true`, создает тег

**Формат тега:**
`v{VERSION}-build-{BUILD_NUMBER}`

---

### 🏪 Работа с App Store (Fastfile_appstore)

#### `auto_release_branch`
Автоматически создает release ветку с правильной версией.

```bash
fastlane auto_release_branch
```

**Что делает:**
1. Проверяет версии в TestFlight и App Store
2. Определяет следующую версию
3. Создает ветку `release/{version}`
4. Обновляет метаданные

---

#### `pass_to_review`
Отправляет приложение на рецензирование в App Store.

```bash
fastlane pass_to_review
```

**Что делает:**
1. Находит последнюю сборку в TestFlight
2. Заполняет информацию о compliance
3. Отправляет на рецензирование
4. Объединяет release ветку в main

**Требует:**
- Активная сборка в TestFlight
- Заполненные метаданные

---

#### `update_metadata_year`
Обновляет год в метаданных приложения.

```bash
fastlane update_metadata_year
```

**Что делает:**
- Автоматически обновляет год в файлах:
  - `name.txt`
  - `copyright.txt`
  - `keywords.txt`
  - `description.txt`
  - `promotional_text.txt`

---

#### `merge_and_delete_release_branch`
Объединяет release ветку в main и удаляет её.

```bash
fastlane merge_and_delete_release_branch
```

**Что делает:**
1. Переключается на main
2. Объединяет release ветку
3. Удаляет release ветку локально и удаленно

---

### 🔧 Утилиты (Fastfile)

#### `clear_keychain`
Очищает keychain и provisioning profiles.

```bash
fastlane clear_keychain
```

---

#### `register_new_device`
Регистрирует новое устройство в App Store Connect.

```bash
fastlane register_new_device
```

**Что делает:**
1. Интерактивно запрашивает имя и UDID устройства
2. Регистрирует устройство через API
3. Перегенерирует development provisioning profiles

---

## 🔄 Workflow примеры

### Workflow 1: Новая сборка для Internal Testers

```bash
# 1. Сборка приложения
fastlane build

# 2. (Опционально) Загрузка символов
fastlane upload_dsyms firebase:true

# 3. Загрузка в TestFlight для internal testers
fastlane upload_testflight

# 4. (Опционально) Создание тега
fastlane tagging tags:true
```

---

### Workflow 2: Новая сборка для External Testers

```bash
# 1. Сборка приложения
fastlane build

# 2. Загрузка символов
fastlane upload_dsyms firebase:true

# 3. Загрузка в TestFlight для external testers
fastlane upload_external_testflight groups:"External Public Beta"

# 4. Создание тега
fastlane tagging tags:true
```

**Примечание:** Первая сборка версии потребует рецензирования Apple (24-48 часов).

---

### Workflow 3: Публикация в App Store

```bash
# 1. Автоматическое создание release ветки
fastlane auto_release_branch

# 2. Сборка приложения
fastlane build

# 3. Загрузка в TestFlight
fastlane upload_external_testflight groups:"External Public Beta"

# 4. После тестирования - отправка на рецензирование
fastlane pass_to_review
```

---

### Workflow 4: Добавление нового устройства

```bash
# 1. Регистрация устройства
fastlane register_new_device
# Введите имя и UDID устройства

# 2. Перегенерация development профилей
fastlane match_generate_dev
```

---

## 🔐 Переменные окружения

### Обязательные переменные

```bash
# Идентификатор приложения
export APP_IDENTIFIER="com.company.appname"

# Основной target
export MAIN_TARGET="AppName"

# Версия приложения
export APP_VERSION="1.2.3"

# Номер сборки (обычно из CI/CD)
export CI_PIPELINE_IID="123"
# или
export BUILD_NUMBER="123"

# API ключи App Store Connect
export APPSTORE_KEY_ID="ABC123XYZ"
export APPSTORE_ISSUER_ID="12345678-1234-1234-1234-123456789012"
export APPSTORE_KEY_CONTENT="$(cat key.p8 | base64)"
```

### Опциональные переменные

```bash
# Файл проекта (по умолчанию {MAIN_TARGET}.xcodeproj)
export MAIN_PROJECT_FILE="AppName.xcodeproj"

# Пароль для match
export MATCH_PASSWORD="your_match_password"

# Настройки AppMetrica
export APPMETRICA_KEY="your_appmetrica_key"
export APPMETRICA_HELPER_PATH="./helper"

# Загрузка dSYM (по умолчанию включено)
export UPLOAD_DSYMS="true"

# Группы external testers по умолчанию
export EXTERNAL_TESTFLIGHT_GROUPS="External Public Beta"
```

### Настройка для CI/CD

Создайте файл `.env` или настройте переменные в вашей CI/CD системе:

```bash
# .env файл (НЕ коммитьте в git!)
APP_IDENTIFIER=com.company.appname
MAIN_TARGET=AppName
APP_VERSION=1.2.3
APPSTORE_KEY_ID=ABC123XYZ
APPSTORE_ISSUER_ID=12345678-1234-1234-1234-123456789012
APPSTORE_KEY_CONTENT=base64_encoded_key_content
MATCH_PASSWORD=secure_password
```

---

## 🐛 Troubleshooting

### Проблема: "Не найдено ни одного IPA файла"

**Решение:**
```bash
# Убедитесь, что вы выполнили сборку
fastlane build

# Проверьте наличие файлов
ls -la fastlane/artifacts/
```

---

### Проблема: "Группа external testers не найдена"

**Решение:**
1. Убедитесь, что группа существует в App Store Connect
2. Проверьте точное название группы (учитывается регистр)
3. Создайте группу вручную, если её нет:
   - App Store Connect → TestFlight → External Testing
   - Нажмите "+" для создания группы

---

### Проблема: "Сертификат не найден"

**Решение:**
```bash
# Установите сертификаты
fastlane match_install_appstore

# Или перегенерируйте их
fastlane match_generate_appstore
```

---

### Проблема: "API ключ недействителен"

**Решение:**
1. Проверьте правильность `APPSTORE_KEY_ID` и `APPSTORE_ISSUER_ID`
2. Убедитесь, что `APPSTORE_KEY_CONTENT` в формате Base64
3. Проверьте права доступа ключа в App Store Connect

---

### Проблема: "Версия уже существует в App Store"

**Решение:**
Используйте новую версию для external testers:
```bash
export APP_VERSION="1.2.4"  # Увеличьте версию
fastlane build
fastlane upload_external_testflight
```

---

## ✅ Best Practices

### 1. Управление версиями

- Используйте семантическое версионирование (MAJOR.MINOR.PATCH)
- Инкрементируйте build number для каждой сборки
- Используйте git теги для версий

### 2. Сертификаты

- Всегда используйте `match` для управления сертификатами
- Не коммитьте сертификаты в основной репозиторий
- Храните пароль match в безопасном месте (CI/CD secrets)

### 3. TestFlight

- Используйте internal testers для быстрого тестирования
- Используйте external testers для публичного бета-тестирования
- Всегда указывайте changelog для external testers

### 4. CI/CD

- Используйте переменные окружения для всех секретов
- Кэшируйте match репозиторий между сборками
- Сохраняйте артефакты (IPA, dSYM) для последующего использования

### 5. Обработка ошибок

- Всегда проверяйте логи при ошибках
- Используйте `--verbose` флаг для детального вывода
- Проверяйте статус сборок в App Store Connect

---

## 📚 Дополнительные ресурсы

- [Fastlane документация](https://docs.fastlane.tools)
- [Match документация](https://docs.fastlane.tools/actions/match/)
- [TestFlight документация](https://developer.apple.com/testflight/)
- [App Store Connect API](https://developer.apple.com/documentation/appstoreconnectapi)

---

## 🤝 Поддержка

Если у вас возникли проблемы или вопросы:

1. Проверьте документацию выше
2. Изучите логи выполнения
3. Проверьте настройки переменных окружения
4. Убедитесь, что все зависимости установлены

---

**Версия документации:** 1.0  
**Последнее обновление:** 2024


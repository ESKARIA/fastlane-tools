# Руководство по работе с External Testers в TestFlight

## 📋 Обзор

Это руководство описывает, как использовать fastlane для публикации сборок для external testers в TestFlight.

## 🚀 Быстрый старт

### 1. Загрузка для External Testers

Используйте новый lane `upload_external_testflight`:

```bash
# С группой по умолчанию "External Public Beta"
fastlane upload_external_testflight

# С указанием конкретных групп
fastlane upload_external_testflight groups:"External Public Beta,QA Testers"

# Через переменную окружения
EXTERNAL_TESTFLIGHT_GROUPS="External Public Beta,QA Testers" fastlane upload_external_testflight
```

### 2. Загрузка для Internal Testers

Используйте lane `upload_testflight` (без изменений):

```bash
fastlane upload_testflight
```

## 📝 Основные различия

| Параметр | Internal Testers | External Testers |
|----------|-----------------|------------------|
| Lane | `upload_testflight` | `upload_external_testflight` |
| Распределение | `distribute_external: false` | `distribute_external: true` |
| Группы | Автоматически (все internal) | Требуется указание групп |
| Ограничения | До 100 тестировщиков | До 10,000 тестировщиков |
| Рецензирование | Не требуется | Требуется одобрение Apple |

## ⚙️ Настройка групп External Testers

### Вариант 1: Параметр командной строки

```bash
fastlane upload_external_testflight groups:"Группа 1,Группа 2"
```

### Вариант 2: Переменная окружения

```bash
export EXTERNAL_TESTFLIGHT_GROUPS="External Public Beta,QA Testers"
fastlane upload_external_testflight
```

### Вариант 3: В CI/CD (GitLab CI пример)

```yaml
upload_external_testflight:
  script:
    - export EXTERNAL_TESTFLIGHT_GROUPS="External Public Beta"
    - fastlane upload_external_testflight
```

## 📋 Полный пример workflow

```bash
# 1. Сборка приложения
fastlane build

# 2. Загрузка dSYM файлов (опционально)
fastlane upload_dsyms firebase:true

# 3. Загрузка в TestFlight для external testers
fastlane upload_external_testflight groups:"External Public Beta"
```

## ⚠️ Важные замечания

### Требования для External Testers

1. **Группы должны существовать** в App Store Connect
2. **Первая сборка требует рецензирования** Apple (может занять 24-48 часов)
3. **Changelog обязателен** для external testers
4. **Версия должна быть новой** - нельзя использовать ту же версию, что уже в App Store

### Обработка ошибок

Если группа не найдена, fastlane выдаст ошибку. Убедитесь, что:

- Группы созданы в App Store Connect
- Названия групп указаны точно (учитывается регистр)
- Группы не удалены или заблокированы

## 🔄 Миграция со старого подхода

### Было (старый способ)

```bash
# Старый способ через параметр :public
fastlane upload_testflight public:true
```

### Стало (новый способ)

```bash
# Рекомендуемый способ - отдельный lane
fastlane upload_external_testflight groups:"External Public Beta"
```

Старый способ с `public:true` все еще работает через lane `upload_testflight`, но рекомендуется использовать `upload_external_testflight` для лучшей читаемости и контроля.

## 📚 Дополнительная информация

### Создание групп в App Store Connect

1. Откройте [App Store Connect](https://appstoreconnect.apple.com)
2. Перейдите в TestFlight → Ваше приложение
3. Выберите вкладку "External Testing"
4. Нажмите "+" для создания новой группы
5. Укажите название группы (например, "External Public Beta")

### Проверка статуса сборки

После загрузки сборки для external testers:

1. Первая сборка: статус "Waiting for Review" (ожидание рецензирования Apple)
2. Последующие сборки той же версии: статус "Ready to Submit"
3. После одобрения: статус "Ready to Test"

## 🐛 Troubleshooting

### Ошибка: "Группа не найдена"

```
Решение: Проверьте название группы в App Store Connect и убедитесь, что она существует.
```

### Ошибка: "Сборка уже загружена для этой версии"

```
Решение: Используйте новую версию или новый build number для external testers.
```

### Ошибка: "Не найдено IPA файлов"

```
Решение: Убедитесь, что вы выполнили fastlane build перед upload_external_testflight
```

## 📞 Поддержка

Если у вас возникли проблемы, проверьте:

1. Логи fastlane: `fastlane upload_external_testflight --verbose`
2. Статус в App Store Connect
3. Настройки групп в TestFlight

